server {
    listen 80;
    listen [::]:80;
    server_name wayfare-api.allbyitself.com;

    location /.well-known/acme-challenge/ {
        root /var/www/html;
    }

    location / {
        return 301 https://$host$request_uri;
    }
}